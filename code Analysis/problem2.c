#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{


    char c[100];
    char ch[100];
    char sen[100];
   // scanf("%c",&c);
 
    //fflush(stdin);
     gets(c);
    //fflush(stdin);
    gets(ch);
    //fflush(stdin);
    gets(sen);
    printf("\n\n%s",c);
    printf("\n%s",ch);
    printf("\n%s",sen);
    
    return 0;
}